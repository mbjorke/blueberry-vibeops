Deploy Supabase edge function: $ARGUMENTS

Steps:
1. If $ARGUMENTS is empty, deploy all functions with `supabase functions deploy`
2. Otherwise deploy specific function: `supabase functions deploy $ARGUMENTS`
3. Verify deployment in the Supabase dashboard
4. Test the function if possible
